// Business rules live here as pure functions (no I/O). Phase 1 fills this in:
// resolveTier, selectGifts, suppressLowerTiers, applyDecline, isCampaignActive,
// and per-market threshold resolution. Placeholder export keeps the scaffold typed.
export const CORE_PACKAGE = '@free-gift-engine/core' as const;
