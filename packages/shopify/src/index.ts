// Typed Admin API wrappers live here (Phase 2): products, scoped discount create/reuse,
// OAuth, webhooks. All Shopify/fetch access is centralized here. Placeholder export
// keeps the scaffold typed.
export const SHOPIFY_PACKAGE = '@free-gift-engine/shopify' as const;
