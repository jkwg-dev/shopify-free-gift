// Next.js embedded admin (App Bridge + Polaris) + the /validate route land here
// (Phases 3-4). Placeholder export keeps the scaffold typed.
export const ADMIN_APP = '@free-gift-engine/admin' as const;
