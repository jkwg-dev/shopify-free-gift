# Rust build output (the Shopify CLI rebuilds the wasm from source at deploy via [build].command).
/target/
# Generated by the shopify_function typegen macro for output types (docs: safe to ignore).
*.output.graphql
