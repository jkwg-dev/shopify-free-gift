// Cron that flips campaign active state at startsAt/endsAt using core's schedule check
// (Phase 6). Placeholder export keeps the scaffold typed.
export const SCHEDULER = '@free-gift-engine/scheduler' as const;
