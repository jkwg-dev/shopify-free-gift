# dependencies
node_modules/

# build output
dist/
.next/
out/
# Next.js generates this on build/dev (augments tsconfig too); not committed.
next-env.d.ts

# TypeScript incremental build cache (Next sets incremental:true in apps/admin/tsconfig.json).
*.tsbuildinfo

# turbo / tooling caches
.turbo/
coverage/

# secrets — never commit (see .env.example)
.env
.env.local
.env.*.local

# misc
*.log
.DS_Store

# Shopify CLI: the repo-root shopify.app.toml is the single source of truth (see commit history).
# `shopify app config link` run from apps/admin regenerates a stale nested copy — never commit it.
apps/admin/shopify.app.toml
.shopify/

e2e/.chrome-profile/